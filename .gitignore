.stock_screener.env
*.env
__pycache__/
*.pyc
*.json.gz
alert_log.csv
trade_log.csv
backtest_summary.csv
*.csv
*.ipynb

# Backtest data cache (large; regenerated on demand)
data_cache/
*.pkl

# SEC XBRL caches (~50MB, refetched on demand); dossier markdown stays tracked
research/data/fundamentals/
research/data/sec_ticker_cik.json
research/data/dossier_*.json
research/data/company_profiles.json

# The decision journal stays local. It is the forward test of the judgment
# layer and worth preserving, but it records positions, entry prices, sizing
# and private reasoning, and this repository is public. Back it up somewhere
# private instead.

# Personal files that happen to live in this directory.
*.numbers
send_worldcup_invites.py
