.stock_screener.env
*.env
__pycache__/
*.pyc
*.json.gz
alert_log.csv
trade_log.csv
backtest_summary.csv
*.csv
*.ipynb

# Backtest data cache (large; regenerated on demand)
data_cache/
*.pkl

# SEC XBRL caches (~50MB, refetched on demand); dossier markdown stays tracked
research/data/fundamentals/
research/data/sec_ticker_cik.json
research/data/dossier_*.json

# The journal is the one CSV worth keeping: decisions recorded before outcomes
# are known, which is the only forward test of the judgment layer.
!research/data/decision_journal.csv
